# FloodWatch Monitor — Python dependencies
# Portfolio 3: Render Deployment (SWE40006)

fastapi==0.115.0          # Web framework — async REST API
uvicorn[standard]==0.30.0  # ASGI server — required to run FastAPI on Render
pymongo[srv]==4.7.0        # MongoDB Atlas driver (srv = DNS SRV URI support)
dnspython==2.6.0           # Required for mongodb+srv:// connection strings
python-dotenv==1.0.0       # Loads .env for local development
sse-starlette==2.1.0       # Server-Sent Events support for FastAPI
pydantic==2.7.0            # Data validation for request bodies